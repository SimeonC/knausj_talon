mode: user.wispr_flow_latched
os: mac
-
# Unlatch via escape or second ⌘+F12 is handled by the Swift bridge + socket server
# (Wispr swallows these keys at app level, so Talon key() bindings don't see them)
